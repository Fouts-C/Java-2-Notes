
public class IntArrayBg implements Cloneable {

    //Container class
    //- A class that holds data
    //an IntArrayBag is a cllection of int's

    //Class has a variable arity method (int...) /Takes as many int's as the user wants to put in
    //(Can only be used on the rightmost parameter)
    
}
