
public abstract class abstractC {
    
    //Abstract method (Class has to be abstract to have an abstract method)
    public abstract void makeNoise();

    //You can't instantiate an abstract class

    //instanceof can be used on an object to see whether or not the object is a type that implements the Pet interface

    //if (a instanceof Pet)    **HOW TO USE**

    //Italics or <<>> means the class is abstract in a uml diagram
    //The dashed arrows mean that the class implements the interface
    //While the solid lines means extends
}

//An interface is a completly abstract class
public interface InnerabstractC {

    /**
     * 
     */
    public void play();
    
}
