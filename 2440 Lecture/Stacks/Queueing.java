package Stacks;

public class Queueing {

    //First in first out data structure (A line)
    //Peek/Examine returns the first item in a queue
    //Remove/dequeue removes the from the front of the queue
    //Add/enqueue - adds to the rear of the queue

    //No queue class in java but there is an interface
    Queue<Integer> q = new LunkedList<Integer>();
    
    //Radix sort can be used to sort data such as phone nums, zip codes, id's
    //Anything that has a fixed amouunt of numbers
    //Made by creating an array of queues
}
